export interface INavState {
    navState: string;
}

export interface IScrollHeight {
    scrollHeight: number;
    currentY: number;
}

export interface IWallet {
    account: string;
    balance: string;
}
