import { motion } from 'framer-motion';
import styled from 'styled-components';

export const LazyImg = styled(motion.img)`
    position: absolute;
    left: 0;
    top: 0;

    width: 100%;
    height: 100%;
`;
