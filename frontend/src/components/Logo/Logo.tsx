import React from 'react';
import * as s from './Logo.style';

const Logo = () => {
    return (
        <s.LogoLink to="/">
            <s.Logo>Logo</s.Logo>
        </s.LogoLink>
    );
};

export default Logo;
